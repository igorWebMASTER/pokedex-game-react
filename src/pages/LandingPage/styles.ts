import styled from 'styled-components';

export const ContainerHomeScreen = styled.div`
]   height: 100vh;
    width: 100vh;
    background: url(${require('../../assets/images/background.png')}) no-repeat center center fixed;
    display: flex;
    justify-content: center;
    align-items: center;
`;